import { InfrastructurePreparationContent } from "../../components/preparation-content";

export const metadata = {
  title: "Infrastructure preparation | Power Champion",
  description: "Qualified public infrastructure context for the Power Champion launch site; not a live deployment status.",
};

export default function InfrastructurePage() {
  return <InfrastructurePreparationContent />;
}
